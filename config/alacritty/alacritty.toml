# Alacritty Terminal Config
# Windows path: %appdata%/alacritty/alacritty.toml
# by: devin dwight 24-11-11

# Alacritty config (https://alacritty.org/config-alacritty.html)
# Alacritty themes (https://github.com/alacritty/alacritty-theme)
# TOML homepage (https://toml.io/en/v1.0.0) 

###########
# GENERAL #
###########
[general]
import = [
	# DEVS THEMES
	#"themes/bonepaper_dark.toml"
	#"themes/bonepaper_light.toml"


	#"themes/martin_dark.toml"
	#"themes/martin_light.toml"


	#"themes/azu.toml"
	#"themes/gnome_terminal.toml"	# NICE!

	# Cobalt (Bluish + Selenized Light combo family) retired 2026-08-01 --
	# its dark half's blue-grey character folded into slate_dark.toml,
	# its light half's cream bg folded into flexoki_light.toml.
	#"themes/solarized_osaka.toml"

	#"themes/ubuntu.toml" 			# improve...

	# OUR 5 CUSTOM FAMILIES (Slate/Bonepaper/Flexoki/Martin/Cypress, real
	# colour0=bg values, no tmux hacking needed)
	#"themes/slate_light.toml"
	#"themes/slate_dark.toml"

	#"themes/cypress_dark.toml"		# Bonepaper x Everforest blend
	#"themes/cypress_light.toml"
	"themes/cypress_midnight.toml"	# Cypress Dark, cooler near-black bg variant

	
	#"themes/palenight.toml"		# dark
	#"themes/seashells.toml"		# light blue/tan
	#"themes/dracula.toml"			# classic
	#"themes/cyber_punk_neon.toml"	# colored ↓
	#"themes/inferno.toml" 			# red
	#"themes/rose_pine.toml"		# purple
]

# working_directory = "C:/Users/devin/"  # Default: "None"
working_directory = "C:/bin/"  # Default: "None"

# for powershell 7 (run "alacritty migrate" first)
[terminal.shell]
program = "C:\\Program Files\\PowerShell\\7\\pwsh.exe"
args = ["-NoLogo"]


##########
# WINDOW #
##########
[window]

# Opacity
opacity = 1.00  # Default: 1.0

# Padding
padding = { x = 10, y = 10 }  # Default: x = 0, y = 0

# Window title (fallback, shown until the shell sets its own via OSC 0 --
# see config/bash/bashrc's PROMPT_COMMAND)
title = "Alacritty - Terminal"  # Default: "Alacritty"

# Dynamic title -- lets the shell set user@host:path per window/tab, so
# multiple sessions (e.g. local vs an SSH'd-into remote box) are
# distinguishable in the taskbar instead of all reading "Alacritty - Terminal"
dynamic_title = true  # Default: true


#############
# SCROLLING #
#############
[scrolling]

# Maximum number of lines in the scrollback buffer
history = 100000  # Default: 10000


##########
# CURSOR #
##########
[cursor]

# Cursor style
style = { shape = "Block", blinking = "On" }  # Default: shape = "Block", blinking = "Off"

# Cursor blinking interval in milliseconds
blink_interval = 420  # Default: 750

# Time after which cursor stops blinking, in seconds
blink_timeout = 0  # Default: 5

[[keyboard.bindings]]
key = "Return"
mods = "Shift"
chars = "\u001B\r"


